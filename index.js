'use strict'

import { NativeModules } from 'react-native'
// name as defined via ReactContextBaseJavaModule's getName
const DialogProgress = NativeModules.DialogProgress;
module.exports = DialogProgress
